
int prevOn = LOW;
void setup() {
    pinMode(8, INPUT);
    pinMode(12, OUTPUT);
}

void loop() {
    if (digitalRead(8) == HIGH && prevOn == LOW) {
        digitalWrite(12, HIGH);
    } else if (digitalRead(8) == LOW) {
        digitalWrite(12, LOW);
    }
    prevOn = digitalRead(8);
}