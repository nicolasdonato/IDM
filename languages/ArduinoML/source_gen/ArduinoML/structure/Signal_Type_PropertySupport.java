package ArduinoML.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class Signal_Type_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<Signal_Type> constants = ListSequence.fromList(Signal_Type.getConstants()).iterator();
    while (constants.hasNext()) {
      Signal_Type constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }

  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<Signal_Type> constants = ListSequence.fromList(Signal_Type.getConstants()).iterator();
    while (constants.hasNext()) {
      Signal_Type constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }

  public String fromInternalValue(String value) {
    Signal_Type constant = Signal_Type.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
